# Прогресс обучения

Живой дневник. Обновляется после каждого закрытого дня или важной сессии.

---

## Текущий день

**5**

## Текущая тема

Мини-анкета — повторение дней 1–4

## Статус

🔄 в процессе

---

## Что уже понимаю

- Переменные: имя, значение, присваивание через `=`
- Разница между строкой (`"31"`) и числом (`31`)
- `print()` — вывод в терминал; запятая между аргументами добавляет пробел
- Склейка строк через `+` — контроль пробелов и знаков препинания
- `input()` — ввод от пользователя, **всегда** возвращает строку
- `int()` — превращает строку в целое число для арифметики
- Арифметика: `+`, `-`, `*`, `/` (деление может дать дробь)
- `"10" + "3"` → `"103"` (склейка), `10 + 3` → `13` (сложение)
- Запуск: сохранить файл (Ctrl+S) → `python lessons/day_XX.py`
- `if` / `else` — ветвление; сравнения: `>`, `<`, `>=`, `<=`, `==`, `!=`
- `=` — присваивание, `==` — сравнение «равно»
- После `if` / `else` — двоеточие `:` и отступ у кода внутри блока

## Что пока путаю

- (заполнять по ходу Дня 5)

## Последняя выполненная задача

**День 4** — возраст 18+, сравнение двух чисел через `if` / `else`. ✅ закрыт.

## Git / GitHub

✅ **Подключено** (сессия во время Дня 4)

- Remote: `https://github.com/mrprohindei/python-ai-learning.git`
- Ветка: `main`
- Первый коммит: «Начало курса Python: дни 1-3 закрыты, день 4 в процессе»
- Учёба на работе → дом: `git push` / `git pull` или Source Control в Cursor (**Ctrl+Shift+G**)

## Следующий шаг

1. Написать мини-анкету в `lessons/day_05.py`.
2. Проверить в терминале.
3. Сдать по шаблону из `lesson_check_template.md`.
4. После изменений — `git commit` + `git push`.

## Заметки для следующего чата

```
Открой python-ai-learning. Прочитай README.md, progress.md, learning_rules.md.
Текущий день: 5. Тема: мини-анкета (повторение). Статус: в процессе.
Дни 1–4 закрыты. day_05.py ещё не сдан на проверку.
Git: github.com/mrprohindei/python-ai-learning (ветка main).
Соблюдай правила: не давать готовые решения, подсказки только по запросу.
```

---

## История дней

| День | Тема | Статус | Дата закрытия |
|------|------|--------|---------------|
| 1 | `print`, переменные | ✅ закрыт | — |
| 2 | строки, `input` | ✅ закрыт | — |
| 3 | числа, `int()`, арифметика | ✅ закрыт | — |
| 4 | `if / else` | ✅ закрыт | — |
| 5 | повторение, мини-анкета | 🔄 в процессе | — |
| 6 | доп. задачи по слабым местам | ⏳ | — |
| 7 | контрольный день (3 задачи) | ⏳ | — |
